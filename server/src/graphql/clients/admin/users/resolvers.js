/**
 * Admin Users Resolvers (Template)
 * Location: /graphql/clients/admin/users/resolvers.js
 */

export const usersResolvers = {
  Query: {
    // 추후 사용자 관리 쿼리 추가
  },
  Mutation: {
    // 추후 사용자 관리 뮤테이션 추가
  },
};

export default usersResolvers;
