/**
 * Translation Error Logger (Stub)
 * TODO: 구현 필요
 */

export function integrateMissingKeyHandler(i18nInstance) {
  // Stub - 실제 에러 로깅 없이 빈 핸들러 반환
  return null;
}

export function collectObjectReturnError(value, key, namespace) {
  // Stub - 실제 에러 수집 없이 무시
  return;
}

export default {
  integrateMissingKeyHandler,
  collectObjectReturnError
};
